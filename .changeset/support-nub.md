---
"better-pm": minor
---

Add support for [nub](https://nubjs.com/docs), detected via `nub.lock`. Uses pnpm's CLI selector grammar (`-F`, `${pkg}...`) for filtered installs and `nub x` for the `pm x` passthrough.
